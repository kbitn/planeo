<div class="container-fluid">
  <div class="text-center head mt-5">
    <h2>SIGN UP</h2>
  </div>
<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>
    <div class="box-w mt-5">
      <div class="form-inputs">
        <div class="text-center">
          <%= f.input :photo, label: ('<i class="fas fa-camera photo-icon"></i>').html_safe %>
          <%= f.input :photo_cache, as: :hidden %>
        </div>

        <%= f.input :name,
                    required: true,
                    autofocus: true,
                    input_html: { autocomplete: "email" }%>
        <%= f.input :email,
                    required: true,
                    input_html: { autocomplete: "email" }%>
        <%= f.input :password,
                    required: true,
                    hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                    input_html: { autocomplete: "new-password" } %>
        <%= f.input :password_confirmation,
                    required: true,
                    input_html: { autocomplete: "new-password" } %>
      </div>
      <div class="form-actions text-center">
        <%= f.button :submit, "SIGN UP", class: "primary-button w-100" %>
      </div>
    <% end %>
</div>
      <div class="text-center bottom-text pb-2 pt-2">
       <p>Already Have An Account? <strong><%= link_to "Log In", new_user_session_path, class: "text-center bottom-text" %></strong></p>
    </div>

